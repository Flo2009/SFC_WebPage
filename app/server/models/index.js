const User = require('./User');
const Submission = require('./Submission');
const Blog = require('./Blog');
const Job = require('./Job');
const Testimonial = require('./Testimonial'); // Add this line
const CandidateRepo = require('./CandidateRepo'); // 🚀 REGISTER TALENT INDEX
const JobMandate = require('./JobMandate');

module.exports = { User, Submission, Blog, Job, Testimonial, CandidateRepo, JobMandate };

